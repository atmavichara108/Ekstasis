# Тетрадь правок

Сюда корректор записывает предложения с точной цитатой, файлом, строкой,
причиной, типом и чекбоксом `[ ]`. Только автор меняет `[ ]` на `[x]`; curator
применяет исключительно отмеченные пункты после показа diff и подтверждения.
