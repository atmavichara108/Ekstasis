---
description: /критика — честное внешнее и внутреннее чтение выбранного текста.
agent: curator
---

Проведи `/критика` для селектора: `$ARGUMENTS`.

Проверь однозначность селектора и вызови агента `critic`. Сохрани его отчёт
отдельным датированным файлом в `Отзывы/`. Зафиксируй, что любые стилевые выводы
остаются наблюдениями и никогда не преобразуются в правки. Рукопись не меняй.
